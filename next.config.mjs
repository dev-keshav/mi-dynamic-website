/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true,
  // swcMinify: true,
  // trailingSlash: true,
  // output: 'export',
  experimental: {
  },
}

export default nextConfig;
